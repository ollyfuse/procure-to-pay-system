from django.urls import path

# Approval actions are handled through the requests viewset
# No separate approval endpoints needed for Sprint 2
urlpatterns = [
    # Future: approval history endpoints could go here
]
